# budget-planner

## Features

1. Add fix and variable expenses
2. Present these expenses in a table like format as a yearly overview
3. Sort and order the expenses by name, category, type (fix, variable), iteration (once, periodic and value
